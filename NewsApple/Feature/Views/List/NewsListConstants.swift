enum NewsListConstants {
    static let title = "News"
    static let tableViewRowHeight: CGFloat = 100
    
    enum Error {
        static let title = "Error"
        static let button = "Ok"
    }
    
    enum Cells {
        static let newsCellIdentifier = "NewsCell"
    }
}
